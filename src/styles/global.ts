import { globalCss } from "./stitches.config";

export const globalStyles = globalCss({
  body: {
    margin: 0,
    padding: 0,
    overflowX: "hidden",
    overflowY: "scroll",
    fontFamily: "$Baloo",
    background: "$white",
  },
});
